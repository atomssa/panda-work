GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/run/sim_complete.C)
add_test(sim_complete ${PANDAROOT_BINARY_DIR}/macro/run/sim_complete.sh)
SET_TESTS_PROPERTIES(sim_complete PROPERTIES TIMEOUT "400")
SET_TESTS_PROPERTIES(sim_complete PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/run/digi_complete.C)
add_test(digi_complete ${PANDAROOT_BINARY_DIR}/macro/run/digi_complete.sh)
SET_TESTS_PROPERTIES(digi_complete PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(digi_complete PROPERTIES DEPENDS sim_complete)
SET_TESTS_PROPERTIES(digi_complete PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/run/reco_complete.C)
add_test(reco_complete ${PANDAROOT_BINARY_DIR}/macro/run/reco_complete.sh)
SET_TESTS_PROPERTIES(reco_complete PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(reco_complete PROPERTIES DEPENDS digi_complete)
SET_TESTS_PROPERTIES(reco_complete PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/run/pid_complete.C)
add_test(pid_complete ${PANDAROOT_BINARY_DIR}/macro/run/pid_complete.sh)
SET_TESTS_PROPERTIES(pid_complete PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(pid_complete PROPERTIES DEPENDS reco_complete)
SET_TESTS_PROPERTIES(pid_complete PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/run/ana_complete.C)
add_test(ana_complete ${PANDAROOT_BINARY_DIR}/macro/run/ana_complete.sh)
SET_TESTS_PROPERTIES(ana_complete PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(ana_complete PROPERTIES DEPENDS pid_complete)
SET_TESTS_PROPERTIES(ana_complete PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")

GENERATE_ROOT_TEST_SCRIPT(${PANDAROOT_SOURCE_DIR}/macro/run/check_complete.C)
add_test(check_complete ${PANDAROOT_BINARY_DIR}/macro/run/check_complete.sh)
SET_TESTS_PROPERTIES(check_complete PROPERTIES TIMEOUT "200")
SET_TESTS_PROPERTIES(check_complete PROPERTIES DEPENDS ana_complete)
SET_TESTS_PROPERTIES(check_complete PROPERTIES PASS_REGULAR_EXPRESSION "TestPassed;All ok")
